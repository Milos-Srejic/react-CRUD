export function* handleGetPostsWorker(action) {
	console.log(action);
	// try {
	// 	const response = yield call(requestGetPosts);
	// 	// const { data } = response;
	// 	console.log(response);
	// 	// yield put(fetchPostsSuccess(data));
	// } catch (error) {
	// 	// yield put(fetchPostsFailure(error));
	// }
}
